---
# tasks file for roles/common

- name: "Common | Instalar paquetes base (git, htop)"
  ansible.builtin.apt:
    name: " {{ item }}"
    state: present
    update_cache: yes
  loop: "{{ common_packages }}"